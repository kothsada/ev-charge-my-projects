apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: panda-ev-dev

resources:
  - ../../base

patches:
  - target:
      kind: Deployment
      name: panda-gateway-api
    patch: |-
      - op: replace
        path: /spec/template/spec/containers/1/args/4
        value: "pandaev:asia-southeast1:panda-ev-core-instance-db-a1?port=5432"

configMapGenerator:
  - name: panda-gateway-api-config
    behavior: merge
    literals:
      - NODE_ENV="development"
      - REDIS_URL="redis://redis-service.panda-ev-dev.svc.cluster.local:6379"
