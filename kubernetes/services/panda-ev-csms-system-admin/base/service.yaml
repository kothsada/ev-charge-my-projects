apiVersion: v1
kind: Service
metadata:
  name: panda-system-api-service
  labels:
    app: panda-system-api
  annotations:
    cloud.google.com/backend-config: '{"default": "panda-system-api-backendconfig"}'
spec:
  type: NodePort
  selector:
    app: panda-system-api
  ports:
    - name: http
      port: 80
      targetPort: 4000
